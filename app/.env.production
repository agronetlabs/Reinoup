# Variaveis de build do Vite para producao. ESTE ARQUIVO VAI PARA O GIT.
#
# Por que a chave pode ser versionada: a publishable key e publica por design.
# Ela e embutida no JS compilado e qualquer visitante consegue le-la abrindo o
# DevTools -- commitar nao muda nada na exposicao. Quem protege os dados e o
# RLS, que filtra toda consulta pela familia do usuario logado.
#
# Sem estas duas, o Vite compila o app com o Supabase DESLIGADO: o login vira
# mock e o paywall passa a confiar no localStorage, ou seja, da para 'assinar'
# editando o navegador.
#
# O que NUNCA entra aqui: service_role, chave do Stripe, token do PagBank.
# Essas sao de runtime e vivem nas variaveis de ambiente do Cloudflare.

VITE_SUPABASE_URL=https://whjfaukvqutgmyozfesb.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_LyJCc3d6xCO3oTF8XtPHbQ_A28AG_Y2
